"""Package root for the NewsFlow AI Editor backend.

This file intentionally left blank. It signals to Python that the
`newsflow_backend` directory should be treated as a package. All
exposed functionality lives in submodules such as `main`, `models`,
`schemas` and `crud`.
"""